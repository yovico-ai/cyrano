{
  "name": "cyrano-client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Strict-TS browser-side runtime for the clientless SSL VPN rewriter.",
  "scripts": {
    "build": "vite build",
    "build:watch": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "happy-dom": "^15.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "acorn": "^8.16.0",
    "astring": "^1.9.0"
  }
}
